server:
  port: 3000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lvchunyujun/spring-cloud-config.git
          username: 1097957468@qq.com
          password: xunyu159
      label: master
      profile: dev
      uri: http://localhost:3000

eureka:
  instance:
    hostname: spring-config-server
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8686/eureka/


